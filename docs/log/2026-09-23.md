# 작업 로그 — 2026-09-23

## 요약

Phase 2 를 5개 하위 단계(2-A ~ 2-E)로 나누고, 첫 단계인 **2-A(면적·길이, 공간 연산, Undo/Redo)** 의
설계 문서와 구현 계획을 작성했다. **코드는 아직 바꾸지 않았다.**

| 커밋 | 내용 |
|---|---|
| `b6b74fe` | docs: Phase 2-A 설계 — `docs/superpowers/specs/2026-09-23-phase2a-geo-ops-undo-design.md` |
| `5963b27` | docs: Phase 2-A 구현 계획 — `docs/superpowers/plans/2026-09-23-phase2a-geo-ops-undo.md` |

---

## 1. 공용 DB 질문

배포본(https://rose-brown.github.io/post-map/)에서 사용자 입력을 공용 DB 에 저장·공유하는 방법을 검토했다.

- 현재는 Phase 1 이라 데이터가 **각자 브라우저의 IndexedDB 에만** 있다. 같은 링크여도 서로 안 보인다.
- 방법은 PRD 의 Phase 3 계획 그대로 **Supabase**(Postgres + Auth + RLS + Realtime).
  `db/repo.ts` 의 공개 함수는 유지하고 내부만 Supabase 로 바꾸면 저장 게이트(불변 규칙 1)도 유지된다.
- 결정이 필요한 항목(보류): 쓰기 권한 범위(익명/로그인/초대), 실시간 반영 여부, IndexedDB 이관 여부.
- 주의: 검색·지오코딩은 dev 프록시라 배포본에서 동작하지 않는다. VWorld 약관 미확인 상태에서
  공용 DB 저장은 재배포 문제가 될 수 있다.
- **결론: PRD 순서대로 Phase 2 를 먼저 진행한다.**

## 2. Phase 2 범위 확인과 분할

출처: `docs/prompts/03-Phase2-레이어-공간연산-프롬프트.md` (완료 기준 8개).

| 단계 | 내용 | 완료 기준 |
|---|---|---|
| **2-A** | 면적·길이, 버퍼·불리언 연산, Undo/Redo | 3·4·5 |
| 2-B | 테이블 뷰 | 8 |
| 2-C | 파일 업로드·좌표계 변환 | 1·2 |
| 2-D | 배경지도 확장 | 6 |
| 2-E | 경로 (제공자 약관 조사부터) | 7 |

## 3. 2-A 브레인스토밍 결정

| # | 결정 |
|---|---|
| D1 | Undo 대상은 **지오메트리만** (생성·삭제·편집·연산·동심원). 텍스트는 입력칸 기본 Ctrl+Z |
| D2 | 연산 결과는 **일반 도형**. `derivedFrom` 은 출처 기록일 뿐, 원본과 연동하지 않는다 |
| D3 | 이항 연산은 **정보 페이지에서 시작 → 지도에서 대상 클릭**. 차집합 = 현재 − 클릭한 도형 |
| D4 | 합집합은 한 번에 2개 (PRD "2개 이상"을 좁힘) |
| D5 | 결과는 첫 도형의 레이어에, 제목은 `buffer(공원, 300m)` 같은 생성 방식 문자열 |
| D6 | 순수 함수는 Node 내장 `node:test`, UI 는 Playwright MCP 로 검증 (새 의존성 없음) |

## 4. 코드 조사에서 새로 확인한 사실

- **Terra Draw 는 MultiPolygon·구멍 있는 Polygon 을 거부한다** (`Feature has holes` / `Feature is not a Polygon`, dist 소스 확인).
  합집합·차집합 결과가 자주 이 형태다 → 별도 MapLibre 소스 `shapes` 로 그리고 선택만 허용, 꼭짓점 편집은 없음.
- 동기화 이펙트는 추가·고아 제거만 하고 **지오메트리 변경을 TD 에 되밀지 않는다** → 편집 Undo 를 위해 갱신 단계 추가 필요.
- 기존 Ctrl+Z 핸들러가 모드와 무관하게 TD 자체 `draw.undo()` 를 부른다 → 작도 중엔 TD, 선택 모드엔 스토어 Undo 로 분기.
- `repo.flush()` 가 피처 삭제 시 blobs 도 지운다 → 삭제 기록에 Blob 을 담아 두고 Undo 때 복원.
- `!f.derivedFrom` 이 "링이 아님" 의미로 쓰여 있다 (`FeatureList`, `LayerPanel`, `MapView`) → `isRing()` 으로 교체해야 연산 결과가 사라지지 않는다.
- 동심원 링 id 가 무작위라 중심 드래그 후 Undo 하면 고아 링이 남는다 → `rng_${centerId}_${radius}` 로 결정적 id.
- Turf 7.4 실측: 떨어진 합집합 → `MultiPolygon`, 안 겹친 교차·완전히 덮인 차집합 → `null`, 과도한 음수 버퍼·선의 음수 버퍼 → `undefined`.
- Node v24.18.0 은 `.ts` 를 직접 실행한다 → 계산 함수 TDD 를 의존성 없이 할 수 있다.

## 5. 구현 계획 (9개 태스크)

1. `lib/geo` 측정 함수 + `node:test` 기반 + rings 이동
2. 버퍼·합집합·교차·차집합 순수 함수
3. 타입 확장·`isRing` 정리·정보 페이지 측정 표시
4. Undo 스택 (순수)
5. 스토어 연결 — 생성·삭제·동심원 Undo + 툴바 버튼
6. 편집 Undo — 드래그 합치기, TD 역반영, 단축키 통합
7. `shapes` 소스와 선택
8. `pendingOp` · OperationPanel · 미리보기 · 대상 선택
9. 회귀 점검(빌드·저장 게이트·모바일)과 CLAUDE.md 반영

## 다음에 할 일

- 구현 방식 선택: 서브에이전트 방식(추천) 또는 이 세션에서 직접.
- 계획서의 태스크 1 부터 순서대로 실행.
- 미해결: `docs/screenshots/deployed-mobile.png` 가 커밋되지 않은 채 남아 있다.
